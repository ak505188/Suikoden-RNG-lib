export default class BasicArea {
  constructor(encounterRate, encounterTableSize, areaType) {
    this.encounterRate = encounterRate;
    this.encounterTableSize = encounterTableSize;
    this.areaType = areaType
  }
}
